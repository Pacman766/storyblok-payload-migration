{
  "name": "@repo/migration",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "npm run migrate -w @repo/cms",
    "reset": "npm run reset -w @repo/cms",
    "migrate:fresh": "npm run reset -w @repo/cms && npm run migrate -w @repo/cms"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "payload": "^3.85.1",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
